name: hello-world
on: 
  push:
    branches:    
      - master
    paths:
      - '.github/workflows/**'
  
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: | 
          echo "Hello World! ++++++++++"
